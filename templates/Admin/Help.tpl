{include file='Admin/Header.tpl' icon='help' __title='Help'}

<p>{gt text='This module generates event-tickets. It handles both the generation and the depreciation.'} {gt text='It is designed to work as an helper module for other modules, to give them an easy way to generate tickets.'} {gt text='Currently no other module uses the TicketManager, but in future they could.'} {gt text='For now, you are able to generate and depreciate tickets via the graphical admin view.'}</p>

{include file='Admin/Footer.tpl'}
